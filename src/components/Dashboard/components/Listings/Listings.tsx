import React from 'react';
import styles from './Listings.module.css';

const Listings: React.FC = () => (
  <div className={styles.Listings} data-testid="Listings">
    Listings Component
  </div>
);

export default Listings;
